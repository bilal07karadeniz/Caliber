# ===========================================
# Frontend Environment Variables
# ===========================================

# API base URL (proxied to backend via netlify.toml redirects)
# Always /api - do NOT put the full backend URL here
VITE_API_URL=/api
