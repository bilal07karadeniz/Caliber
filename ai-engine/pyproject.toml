[project]
name = "caliber-engine"
version = "1.0.0"
description = "Caliber - AI Engine for Job Matching"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
