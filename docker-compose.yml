version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: caliber-db
    environment:
      POSTGRES_DB: caliber
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - caliber-network

  redis:
    image: redis:7
    container_name: caliber-redis
    ports:
      - "6379:6379"
    networks:
      - caliber-network

  server:
    build: ./server
    container_name: caliber-server
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/caliber
      JWT_SECRET: dev-jwt-secret
      JWT_REFRESH_SECRET: dev-jwt-refresh-secret
      AI_SERVICE_URL: http://ai-engine:8000
      CLIENT_URL: http://localhost:5173
      PORT: 3001
    depends_on:
      - postgres
      - redis
    volumes:
      - ./server/src:/app/src
      - ./uploads:/app/uploads
    networks:
      - caliber-network

  ai-engine:
    build: ./ai-engine
    container_name: caliber-ai
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/caliber
      SPACY_MODEL: en_core_web_md
    depends_on:
      - postgres
    volumes:
      - ./ai-engine/app:/app/app
      - ./uploads:/app/uploads
    networks:
      - caliber-network

  client:
    build: ./client
    container_name: caliber-client
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:3001/api
      VITE_AI_SERVICE_URL: http://localhost:8000
    depends_on:
      - server
    volumes:
      - ./client/src:/app/src
    networks:
      - caliber-network

volumes:
  postgres_data:

networks:
  caliber-network:
    driver: bridge
